import React from "react";
import { Box } from "@mui/material";

import "./Settings.scss";

const Settings: React.FC = () => {
  return (
    <Box className="at-settings">
      <h4>Settings</h4>
    </Box>
  );
};

export default Settings;
