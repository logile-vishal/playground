import React from "react";
import { Box } from "@mui/material";

import "./Schedule.scss";

const Schedule: React.FC = () => {
  return (
    <Box className="at-schedule">
      <h4>Schedule</h4>
    </Box>
  );
};

export default Schedule;
