import React from "react";
import { Box } from "@mui/material";

import "./Notifications.scss";

const Notifications: React.FC = () => {
  return (
    <Box className="at-notifications">
      <h4>Notifications</h4>
    </Box>
  );
};

export default Notifications;
