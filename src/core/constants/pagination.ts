export const PAGINATION_EVENT_TYPE = {
  prev: "prev",
  next: "next",
  pageSize: "pageSize",
  currentPage: "currentPage",
} as const;

export const PAGINATION_SIZE = {
  small: "small",
  large: "large",
} as const;
