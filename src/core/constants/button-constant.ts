import type { ButtonSeverity } from './../types/button.type';

export const BUTTON_SEVERITY: Record<ButtonSeverity, ButtonSeverity>= {
    primary: 'primary',
    secondary: 'secondary',
    destructive: 'destructive',
}